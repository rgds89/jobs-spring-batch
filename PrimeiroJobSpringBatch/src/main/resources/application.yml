spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jobs_spring_batch?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: root1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    initialize-schema: always